# Domain notes for agents

Single-context repository. The ubiquitous language lives in
[`../../CONTEXT.md`](../../CONTEXT.md); decisions of record live in [`../adr/`](../adr/). Read
both before implementing.

Fill this file with the domain orientation an agent needs that does not belong in the glossary
— external systems and their conventions, units, and gotchas — as the project takes shape.
